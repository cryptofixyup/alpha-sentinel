# Proposals

Untrusted transition requests enter here.

A proposal is data, not authority. It cannot mutate durable state, bypass policy, authorize itself, or implicitly trigger a security-critical transition.
